---
export async function getStaticPaths() {
  // Define your case studies data here
  const caseStudies = [
    { slug: 'e-commerce-platform', title: 'E-commerce Platform Redesign' },
    { slug: 'mobile-app', title: 'Mobile App Development' },
    { slug: 'web-optimization', title: 'Web Performance Optimization' }
  ];

  return caseStudies.map(study => ({
    params: { slug: study.slug },
    props: { title: study.title }
  }));
}

const { slug } = Astro.params;
const { title } = Astro.props;
---
<h1>{title}</h1>
<p>This is a detailed case study for {slug}.</p>
